# Site folder

This folder contains the static site files used for Project 1 — NGINX Hosting.

Files:
- `index.html` — main page
- `style.css` — basic styles

To preview locally, the files are copied to the NGINX root (`/var/www/html`) by running `deploy.sh`.
